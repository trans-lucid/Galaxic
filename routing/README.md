# Routing

Caddy is the default local router because it is simple and easy to template.

Traefik can be added later for heavier microservice, Kubernetes, or DevOps
profiles. Generated repos should include a role-specific `routing/Caddyfile`.
